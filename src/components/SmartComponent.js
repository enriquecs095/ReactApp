import React from "react";

export default function SmartComponent(props) {
  let value1 = 1;
  let value2 = 5;
  let value3 = 10;
  let value4 = 100;
  return <div></div>;
}

/*
    <div>
      
      <button onClick={"/increment/" + value1.slug}>+1</button>
      <button onClick={"/increment/" + value2.slug}>+5</button>
      <button onClick={"/increment/" + value3.slug}>+10</button>
      <button onClick={"/increment/" + value4.slug}>+100</button>
    </div>*/
